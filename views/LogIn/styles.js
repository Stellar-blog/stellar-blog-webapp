import styled from 'styled-components'

export const GuestLoginBtn = styled.div`
    padding: 0.4rem 1rem;
    margin: 1rem 0;
    border-radius: 0.2rem;

    color: white;
    cursor: pointer;

    background: #814f5d;
`
